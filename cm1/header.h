#include <stdio.h>
#include <fstream>
#include <io.h>
#include <vector>
#include <iostream>
using namespace std;
typedef float chtype;

void input(vector <vector <chtype>> &, int *, int *);
void LDU(vector <vector <chtype>> &, int, int);
void L(vector <vector<chtype>> &, int, int,int);
void D(vector <vector<chtype>> &, int, int,int);
void U(vector <vector<chtype>> &, int, int,int);


